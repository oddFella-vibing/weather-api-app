export interface weatherData {
  city: string;
  temperature: string;
  weather_descriptions: string[];
  feels_like: string;
  humidity: string;
  cloudcover: string;
  wind_speed: string;
  weather_icons: string[];
}
